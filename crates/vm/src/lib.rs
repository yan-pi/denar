//! Eager interpreter and Bitcoin glue for btclisp.
//!
//! Placeholder crate: the `eval` loop, opcode handlers, resource limits, and
//! transaction introspection are scheduled for Sprints 4–6. It exists now so
//! the workspace dependency graph matches the spec (§2).

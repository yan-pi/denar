[package]
name = "btclisp-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Core IR types and opcode definitions for btclisp."

[dependencies]
bytes.workspace = true

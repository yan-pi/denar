[package]
name = "btclisp-compiler"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Surface-syntax frontend (lexer, parser, AST) for btclisp."

[dependencies]
btclisp-core.workspace = true
thiserror.workspace = true

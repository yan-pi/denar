//! Binary codec between `btclisp-core`'s `CoreExpr` and bytes.
//!
//! Placeholder crate: the encoder, decoder, and round-trip property tests are
//! scheduled for Sprint 3. It exists now so the workspace dependency graph
//! matches the spec (§2).
